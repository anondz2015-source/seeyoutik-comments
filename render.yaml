services:
  - type: web
    name: seeyoutik-comments
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 0
    envVars:
      - key: EULER_API_KEY
        sync: false
